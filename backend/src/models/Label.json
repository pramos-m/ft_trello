{
  "bsonType": "object",
  "required": ["_id", "name", "color", "taskId"],
  "properties": {
    "_id": {
      "bsonType": "objectId",
      "description": "Label Id, required"
    },
    "name": {
      "bsonType": "string",
      "description": "Name of the label, required"
    },
    "color": {
      "bsonType": "string",
      "description": "Color of the label, required"
    },
	  "taskId": {
		  "bsonType": "objectId",
		  "description": "Task Id to which the label belongs, required"
	  }
  },
  "additionalProperties": false
}
